<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
        
    body
        h2 Confirmar cuenta UpTask
        p Ya estás casi inscrito, solo preciona el siguiente botón.
        a(href=`${confirmarUrl}`) Confirmar Cuenta
        p Si no puede acceder a este enlace visita #{confirmarUrl}
